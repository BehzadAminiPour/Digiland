import heroImg from "../assets/heroImg2.jpg";

export default function Hero() {
  return (
    <section>
      <div>
        <img src={heroImg} alt="hero images" />
      </div>
    </section>
  );
}
